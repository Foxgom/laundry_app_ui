import 'package:flutter/material.dart';

class RincianScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Detail Order")),
      body: Center(child: Text("Ini halaman rincian.")),
    );
  }
}
